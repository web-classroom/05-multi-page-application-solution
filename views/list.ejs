<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dictons | Liste</title>
  </head>
  <body>
    <h1>Liste</h1>
    <ul>
      <% rows.forEach(function(row) { %>
      <li>
        <a href="/<%= row.id %>"><%= row.dicton %></a>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
